flutter_native_splash:
  android: true
  ios: true
  web: false
  color: "#141217"
  image : "assets/images/logo_splash.png"
  # branding : "assets/images/splash.png"
  # branding_dark : "assets/images/splash.png"
  android_12:
    color: "#141217"
    image :  "assets/images/logo_splash.png"
    # branding: "assets/images/splash.png"
    # branding_dark : "assets/images/splash.png"

# when make the splash view logo make the background is 960*960 pixels ,
#  and make the circle in the middle with 640 pixels and 
#  put the logo in the circle in the middle 
#install :: 
# dart run flutter_native_splash:create --path=path/to/my/file.yaml